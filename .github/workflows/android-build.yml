name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Install Rust Android targets
      run: |
        rustup target add aarch64-linux-android
        # Add other Android targets if needed
        rustup target add armv7-linux-androideabi
        rustup target add i686-linux-android
        
    - name: Build Rust library with cargo-ndk
      run: |
        # Install cargo-ndk for easier Android builds
        cargo install cargo-ndk
        cargo ndk -t arm64-v8a build --release
        
        # Copy the built library
        mkdir -p android/app/src/main/jniLibs/arm64-v8a/
        cp target/aarch64-linux-android/release/libaudiovisualizer.so android/app/src/main/jniLibs/arm64-v8a/

    - name: Debug Build Variants
      run: |
        cd android
        echo "=== Build Types ==="
        ./gradlew :app:printBuildTypes
    
        echo "=== Product Flavors ==="  
        ./gradlew :app:printProductFlavors
    
        echo "=== All Tasks ==="
        ./gradlew :app:tasks | grep -i assemble | head -10
           
    - name: Build Android APK
      run: |
        cd android
    
        # List all variants
        ./gradlew :app:tasks --all | grep -i variant
    
        # Try building the app module directly
        ./gradlew :app:assemble
        # OR
        ./gradlew :app:assembleRelease
           
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: audiovisualizer-apk
        path: android/app/build/outputs/apk/debug/app-debug.apk
